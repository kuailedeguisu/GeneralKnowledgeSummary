1.多进程情况的前后台切换
jobs 查看有哪些后台运行的进程，包含id
ps 查看所有进程，没有id
fg %id 将后台运行的进程放在前台，可以交互输入和输出
bg %id 将前台运行的进程放在后台，然后进入别的进程，可以实现别的进程的交互输入和输出，不占用shell而不能做别的事情


2.gcc各个参数的含义

3.ps各个参数的含义

4.CMake和autoconf区别

5.Linux的可执行文件是什么格式的：elf文件

6.ls各个参数的含义

7.查看网络地址的命令
ifconfig
netstat

8.如何配置DNS
