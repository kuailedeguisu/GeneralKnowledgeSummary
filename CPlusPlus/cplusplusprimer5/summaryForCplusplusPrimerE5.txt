1.基本概念：
对象：具有某种数据类型的内存空间

可寻址的最小内存单元叫字节（byte），8 bits

C++是一种静态数据类型，在程序编译的时候进行数据类型检查。

unsigned int 和int类型进行运算后，会将int类型的负数变为unsigned int处理

未初始化的变量：
内置类型变量在函数体外，默认初始化为0；
内置类型变量，在函数体内，未初始化的变量不会默认初始化，如果使用了这个未初始化的变量会导致未定义的行为；而对于自定义的类的对象，其值由类决定
ps：应该总是初始化内置数据类型的变量，防止出现未定义的行为导致的调试困难

变量的声明和定义：
声明：指定类型和名字，可以声明多次
定义：指定类型和名字，同时分配存储空间，一个变量变量只能定义一次
多个文件中用到一个变量：可以在用到这个变量的所有文件中声明它，而在一个文件中定义这个变量

extern类型的变量：
将extern的变量表示声明它而不定义它


名字的作用域：
起于声明的位置，结束于声明语句所在的作用域末端

全局变量：
如果想用全局变量，在函数内部不宜再定义同名局部变量， 因为局部变量会覆盖全局变量的值，除非用：：作用域来访问该全局变量

基本数据类型： 
int char。。。。。

复合数据类型：
引用：对象的别名，包含左值引用和右值引用（c++11引入），声明时必须初始化
指针

2.C++的常见数据结构

简单的内置类型（bool-1, char-1, wchar_t-2, char16_t-2, char32_t-4, short-2, int-2, long-4, long long-8, float-6, double-10, long double-10）(long long:c++11)

字符串string(可变长的字符序列)

向量

数组array

类类型


3.常见问题
一个类没有构造函数，如何构造对象？


